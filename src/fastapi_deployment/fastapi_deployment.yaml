# This file was generated using the `serve build` command on Ray v2.4.0.

import_path: fastapi_deployment:app

runtime_env: {}

host: 0.0.0.0

port: 8000

deployments:

- name: FastAPIDeployment
  route_prefix: /hello
